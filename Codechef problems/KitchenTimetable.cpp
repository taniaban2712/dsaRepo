#include<bits/stdc++.h>
using namespace std;

int main(){
    int t;
    cin>>t;
    while(t--){
        int n{0},a{0};
        cin>>n;//the number of elements in the vector
        vector<int>v1[n];
        vector<int>v2[n];
        /*for(auto &it=v1.begin();it!=v1.end();v1++){
            cin>>a;
            v1.emplace(a);
        }
        for(auto &it=v2.begin();it!=v2.end();v2++){
            cin>>a;
            v2.emplace(a);
        }*/

        for(auto& it:v1){
            
        }

    }
}