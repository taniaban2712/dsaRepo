#include<bits/stdc++.h>
using namespace std;
#define ll long long int
#define sp<<' '
#define n cout<<endl
#define ios ios::sync_with_stdio(false);cin.tie(0);cout.tie(0);
 
#define V(a) ll a;cin >> a
 
#define F(i, n) for (ll i = 0; i < n; i++)
#define N(n) for (n; n > 0; n--)
 
#define T V(t); N(t)
 
#define IA(a, n) for (ll i = 0; i < n; i++)cin >> a[i]
 
#define YN(f) cout << ((f) ? "YES" : "NO")
#define OA(a) for (auto x : a)cout << x sp;


#define tania void solve(void);int main(){ios solve();return 0;}void solve()
//here we are defining the driver code, ie int main() and functions within them and calling the function solve()
//anss we are doing the tasks there.